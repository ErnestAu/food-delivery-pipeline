{
  "type": "object",
  "properties": {
    "decision": {
      "type": "string",
      "enum": ["suggest_fix", "warn_for_review", "pass"]
    },
    "severity": {
      "type": "string",
      "enum": ["high", "warning", "info"]
    },
    "summary": {
      "type": "string"
    },
    "rationale": {
      "type": "string"
    },
    "proposed_sql": {
      "type": "string",
      "description": "Complete raw proposed model SQL for suggest_fix, with no Markdown fence or explanatory prose; an empty string otherwise."
    },
    "clarifying_question": {
      "type": "string",
      "description": "A question for the analyst when the decision is warn_for_review; an empty string otherwise."
    }
  },
  "required": ["decision", "severity", "summary", "rationale", "proposed_sql", "clarifying_question"],
  "additionalProperties": false
}
